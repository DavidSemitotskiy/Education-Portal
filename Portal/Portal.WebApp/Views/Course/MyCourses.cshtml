@using Portal.Domain.Models
@model List<CourseState>

@{
    ViewData["Title"] = "MyCourses";
}

<div>
    @{
        if (Model.Count() == 0)
        {
            <h1>You haven't subscribed on any courses yet</h1>
        }
    }
</div>

