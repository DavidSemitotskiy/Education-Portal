@using Portal.Domain.Models
@model List<Course>
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    @if (Model.Count() == 0)
    {
        <h1>No Courses Available</h1>
    }
</div>
